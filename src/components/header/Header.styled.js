import styled from 'styled-components';

export const Header = styled.div`
z-index: 20;
  display: flex;
  z-index: 20;
  align-items: center;
  justify-content: space-between;
  position: relative;
`;
